import { resolve } from "path"

export const dataFolderPath:   string = resolve(__dirname, "..", "..", "data")
export const inputFolderPath:  string = resolve(__dirname, "..", "..", "syntax")
export const outputFolderPath: string = resolve(__dirname, "..", "..", "..", "syntaxes")
